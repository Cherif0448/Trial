<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Info</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* Full height and centering for the page */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

        /* Header styling */
        header {
            margin-bottom: 40px; /* Space between header and form */
        }

        h1 {
            font-size: 60px; /* Larger font size for the header */
            font-weight: 600;
            background-image: linear-gradient(to left, #553c9a, #b393d3);
            color: transparent;
            background-clip: text;
            -webkit-background-clip: text;
        }

        /* Form styling */
        form#City {
            display: flex;
            flex-direction: column;
            gap: 20px; /* Larger space between input and button */
            align-items: center;
        }

        input {
            padding: 15px;
            font-size: 18px;
            border: 1px solid #ccc;
            border-radius: 8px;
            width: 300px; /* Wider input field */
        }

        button {
            padding: 15px 25px;
            font-size: 18px;
            border: none;
            border-radius: 8px;
            background-color: #5E5DF0;
            color: #FFFFFF;
            cursor: pointer;
            font-family: Inter, Helvetica, sans-serif;
            font-weight: 700;
            width: 100%; /* Button takes full width of the form */
            text-align: center;
        }

        button:hover {
            background-color: #4a4aef; /* Slightly darker color on hover */
        }

        /* Optional output styling */
        #output {
            margin-top: 20px;
            font-size: 20px;
    font-weight: 600;
    background-image: linear-gradient(to left, #553c9a, #b393d3);
    color: transparent;
    background-clip: text;
    -webkit-background-clip: text;
        }
    </style>
</head>
<body>
    <header>
        <h1>
            Get Weather Information from any location you want!
        </h1>


 <script src="/app.js" defer></script>

        
    </header>
    
    <form id="City">
        <input placeholder="City">
        <button>Get Weather</button>
    </form>

    <p id="output"></p>
    <div>
        <a href="/Help">go to help</a>
    </div>
</body>
</html>
